export type FormData = {
  startDate: string;
  stayNights: string;
  departureTime: string;
  disbandTime: string;
};

export type FormErrors = {
  startDate?: string;
  stayNights?: string;
  departureTime?: string;
  disbandTime?: string;
};
